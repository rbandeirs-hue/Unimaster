# ======================================================
# Judo Academy - Dependências Python
# ======================================================

# Framework Web
Flask==3.1.3
Werkzeug==3.1.6

# Autenticação e Sessões
Flask-Login==0.6.3
Flask-WTF==1.2.2

# Banco de Dados MySQL
mysql-connector-python==9.1.0

# Manipulação de Datas
python-dateutil==2.8.2

# Sync currículo Zempo (fetch + parse HTML)
requests==2.33.0
beautifulsoup4==4.12.2

# Leitura de PDF (importar calendário)
pypdf==6.10.2
pdfplumber==0.11.9

# Geração de Excel (XLSX)
openpyxl==3.1.2

# Placar judô em tempo real (eventos_competicoes)
Flask-SocketIO==5.3.6
python-socketio==5.14.0

# Variáveis de ambiente (.env)
python-dotenv==1.2.2

# Servidor WSGI em produção (systemd + Nginx)
gunicorn==23.0.0

# Web Push (notificações no navegador — obrigatório para /push/test e envio real)
pywebpush==2.3.0

# Geração de QR Code (link de pagamento InfinitePay) — usa Pillow já instalado
qrcode==8.2

# ======================================================
# Bibliotecas Padrão (não precisam ser instaladas)
# ======================================================
# - datetime
# - os
# - base64
# - unicodedata
# - json
# - hashlib

# ======================================================
# Instalação:
# ======================================================
# python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Produção: deploy/unimaster.service + gunicorn -c gunicorn.conf.py wsgi:app (ver deploy/PRODUCAO.md)
