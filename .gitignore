# Credenciais e segredos — NUNCA versionar
.env
*.env
Placar/.env
secrets.json
credentials.json

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
env/
.venv/

# Uploads e arquivos gerados
static/uploads/
*.log

# Backups e dumps de banco — nunca versionar
*.sql
backup_*.sql
dump_*.sql

# Configurações locais com credenciais
.claude/settings.local.json
*/.claude/settings.local.json

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# WhatsApp service (Baileys)
whatsapp-service/node_modules/
whatsapp-service/auth/
whatsapp-service/api_key.txt

# Chaves do Web Push (a privada assina as notificações — nunca versionar)
vapid_*.pem

# Capturas de depuração: o HTML salvo do Zempo traz dados de alunos (CPF)
debug_zempo_*.html
*.rar

# Projeto com repositório git próprio — versionado à parte, não aqui dentro
site-sensei-rodrigo-silva/

# Logs rotacionados do gunicorn/nginx (o *.log acima nao pega .log.1 nem .gz)
logs/
!logs/.gitkeep
